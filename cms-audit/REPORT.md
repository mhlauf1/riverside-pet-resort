# Riverside Pet Resort — Sanity CMS Audit

> **SNAPSHOT — captured July 2026.** This audit reflects the codebase and dataset at the time it was written and is NOT kept up to date. Verify any finding against the current code before acting on it.

1. Riverside registers 5 regular document types, 2 settings singletons, and 50 object types (`studio/src/schemaTypes/index.ts`) — the largest schema in the portfolio, because it hosts the Rio Grooming School sub-site.
2. The machine inventory contains every registered field; no registered schema file was omitted (`cms-audit/schema-inventory.json`).
3. Four Portable Text configurations exist (`studio/src/schemaTypes/objects/blockContent.tsx`, `blockContentTextOnly.tsx`, `singletons/settings.tsx` description, and the new `settings.transitionBanner.content`).
4. Seventeen GROQ queries are captured: fourteen exported from `frontend/sanity/lib/queries.ts`, the cross-document `jobListings` subquery embedded in the page-builder expansion, and two inline lookups in `frontend/app/api/contact/route.ts`.
5. Mechanical source diff against Wags found 6 added schema files and 6 variants; 45 of 51 schema files are byte-identical. The documented parentage (wags-stay-n-play) is supported, not contradicted.
6. Production contains 6 pages, 3 services, 10 school pages, 7 suburb location pages, 0 job postings, 1 settings doc, 1 schoolSettings doc, 93 image assets, 0 file assets (read-only GROQ, 2026-07-11).
7. Settings integrity is CLEAN — the first repo in the lineage where it is: one `settings` doc at pinned id `siteSettings`, one `schoolSettings` at `schoolSettings` (`scripts/migrate-settings-id.js` shows deliberate consolidation).
8. ~~Highest risk: `frontend/app/redirect-map.ts` … not wired~~ **CORRECTED (7/26): FALSE POSITIVE.** The map IS wired via `frontend/proxy.ts` — Next 16 renamed `middleware.ts` to `proxy.ts`; the original audit grepped for the old filename. Redirects fire correctly for legacy hosts.
9. Second risk: the canonical production domain is explicitly UNCONFIRMED in source (`frontend/app/site-config.ts`, pending Peter).
10. Third risk: 29 out-of-band production write scripts with the hardcoded project id (`scripts/`), meaning dataset state — not script history — must be treated as the source of truth for migration.

## Phase 0 — Repository orientation

The repository serves Riverside Pet Resort (Hastings, MN), the newest portfolio site, launched ~June 2026, plus the Rio Grooming School & Salon sub-site at `/school/...` (`README.md`, `CLAUDE.md`, `frontend/app/site-config.ts`). The frontend uses Next 16.1.1, React 19.2.3, TypeScript, and the App Router with two route groups carrying separate chrome: `(site)` for the resort and `(school)` for Rio (`frontend/package.json`, `frontend/app/`).

Sanity dependencies match the lineage (`sanity ^5.1.0`, `next-sanity ^12.0.5`, `@sanity/client ^7.13.2`, `@sanity/image-url ^1.2.0`, `@sanity/vision ^5.13.0`, `@sanity/assist ^5.0.3`, `sanity-image ^1.0.0`, Unsplash asset source) plus Riverside-specific additions: `@iconify/react`, `yet-another-react-lightbox`, `@vercel/speed-insights`, `sonner`. Unlike Wags there is no `@next/third-parties` — the GTM/GA4 bootstrap is inlined in `frontend/app/layout.tsx` (the Kingdom pattern).

Project configuration is project `7ze0boy4`, dataset `production`, API version `2025-09-25`; values come from `NEXT_PUBLIC_SANITY_PROJECT_ID` / `NEXT_PUBLIC_SANITY_DATASET` / `NEXT_PUBLIC_SANITY_API_VERSION` (`frontend/.env.example`, `frontend/sanity/lib/api.ts`); tokens by name only (`SANITY_API_READ_TOKEN`, `frontend/sanity/lib/token.ts`). The env example also defines `NEXT_PUBLIC_SITE_URL` (marked unconfirmed) and the SMTP block (`SMTP_HOST=smtp.gmail.com`, `SMTP_PORT=465`, `SMTP_USER`, `SMTP_PASS`, `SMTP_FROM`, `CONTACT_FORM_TO_EMAIL`). Studio exists standalone (`studio/`) and embedded (`frontend/app/studio/[[...tool]]/page.tsx`); both share `studio/src/schemaTypes`. `sanity.schema.json` at the repo root is generated by `sanity schema extract` during frontend typegen (`frontend/package.json` scripts).

`createClient` uses CDN reads **only in production** (`NODE_ENV` check — a deliberate drift from Wags's unconditional `useCdn: true`), a read token, and stega config (`frontend/sanity/lib/client.ts`). `defineLive` supplies `sanityFetch`/`SanityLive` (`frontend/sanity/lib/live.ts`, byte-identical to Wags). All six page routes are server components; static params use published perspective with stega off. No numeric `revalidate` export exists; the contact route's two inline GROQ lookups use `{next: {revalidate: 300}}`.

## Phase 1 — Complete schema extraction

### Type census and parent drift

The full field-by-field census is in `cms-audit/schema-inventory.json`. Registration is authoritative at `studio/src/schemaTypes/index.ts`.

| Kind | Count | Types | Parent result |
|---|---:|---|---|
| document | 5 | page, service, testimonial, **schoolPage**, **jobPosting**, **locationPage** (6 incl. testimonial) | page/service/testimonial byte-identical to Wags; three added |
| singleton | 2 | settings, **schoolSettings** | settings variant; schoolSettings added |
| object | 50 | all entries in JSON `objectTypes` | 44 byte-identical files; variants: contactForm, heroMarquee, heroSplit, infoSection, serviceCards; added: quickSchoolsEnquiry, jobListings |

Mechanical evidence is `diff -rq studio/src/schemaTypes ../wags-stay-n-play/studio/src/schemaTypes`: additions `documents/{schoolPage,jobPosting,locationPage}.ts`, `objects/{jobListings,quickSchoolsEnquiry}.ts`, `singletons/schoolSettings.tsx`; variants `objects/{contactForm,heroMarquee,heroSplit,infoSection,serviceCards}.ts`, `singletons/settings.tsx`, and the registry. This strongly supports the documented clone parent.

Notable exact Riverside deltas:

- `contactForm.destinationEmailOverride` (string, comma-separated multi-recipient, `[EMAIL-TBD]` treated as unset) and per-field `placeholder` (`objects/contactForm.ts`).
- `heroMarquee` gains `backgroundColor` (cream/sand/forest), `tertiaryCta` (button), `belowCtaText`, `showIllustrations` (boolean, off for the Rio marquee), `verticalSpacing` (`objects/heroMarquee.ts`).
- `heroSplit.imageAspect` (square/landscape/wide/portrait) (`objects/heroSplit.ts`).
- `infoSection.body` (plain text, rows 6) added ahead of the retitled rich-text `content` (`objects/infoSection.ts`).
- `serviceCards.cards[].icon` (Iconify name, rendered via `@iconify/react`) (`objects/serviceCards.ts`).
- `settings` gains `transitionBanner` (enabled/content/linkUrl/linkLabel) and **loses `ctmScriptUrl`** — CallTrackingMetrics has no CMS home in this repo (`singletons/settings.tsx`).
- **Reference-inventory correction:** the Wags inventory lists `webcamPreview` in `page`/`service` pageBuilder members, but the byte-identical source files contain no such member (md5 + grep verified in both repos). Corrected in this inventory and flagged for the synthesis pass.

### Portable Text

`blockContent` (byte-identical to Wags: default styles/lists/decorators, custom `link` annotation, hotspot image blocks) now backs nine locations, adding `jobPosting.description`, `jobListings.description`, and `quickSchoolsEnquiry.description`. `blockContentTextOnly` is unchanged. `settings.description` retains the restricted config. New fourth config: `settings.transitionBanner.content` — no styles/lists, strong/em only, no annotations, rendered by `TransitionBanner.tsx` with its own components map.

The custom renderer (`frontend/app/components/PortableText.tsx`, byte-identical to Wags) is used only by `InfoSection.tsx` and `Cta.tsx`. **Seven** render sites use bare `@portabletext/react` with no components map — FaqAccordion, SplitContent, FeatureList, ContentColumns, ContactForm, plus the new QuickSchoolsEnquiry and JobListings — so the known lineage link-rendering defect persists and widened.

### Settings singletons

Every field and its consumers are in JSON `settingsSingleton.fields` (resort) and `subSite.settingsSingleton` (school). Studio pins `siteSettings` and `schoolSettings` (`studio/src/structure/index.ts`); both frontend queries use unordered `[0]`. Operational consumption: metadata/JSON-LD/GTM in `frontend/app/layout.tsx`; resort nav/footer/booking CTAs in `frontend/app/(site)/layout.tsx`; school chrome in `frontend/app/(school)/layout.tsx`.

Hardcoded values that belong in settings: the canonical origin (centralized but UNCONFIRMED in `site-config.ts`), the resort logo `/images/riverside-logo-2026.png` in `Header.tsx:219`/`Footer.tsx:58` — the CMS `settings.logo` field is unset live and unconsumed for display, the **Kingdom lineage defect re-appeared** (the school header consumes its CMS logo correctly at width 360) — calculator prices (`app/data/pricingData.ts`), and the header booking-CTA labels/colors/grooming route (`(site)/layout.tsx`, attributed to Brian 6/26 in comments).

## Phase 2 — Queries and consumption

All seventeen query texts are captured verbatim/expanded in JSON `queries`.

| Query | Consumers | Core effect |
|---|---|---|
| settingsQuery | root layout + (site) layout | spread + resolved nav/footer/CTA links, computed faviconUrl; unordered `[0]` |
| getPageQuery / homepageQuery / pagesSlugs | `/`, `/[slug]` | page identity/SEO + expanded pageBuilder; homepage slug special case |
| getServiceQuery / serviceSlugs / servicesNavQuery | `/services/[slug]`, nav | service pages + sorted nav dropdown |
| getLocationPageQuery / locationPageSlugs | `/locations/[suburb]` | suburb SEO pages; slug mapped to `suburb` param |
| schoolSettingsQuery | (school) layout | school chrome incl. backToResort, hours |
| getSchoolPageQuery / schoolHomeQuery / schoolPageSlugs | `/school`, `/school/[slug]` | school pages; `home` slug renders at `/school` |
| sitemapData | sitemap | slug/noIndex/updated across all four routable types |
| jobListings subquery | any pageBuilder w/ jobListings block | cross-document join: active, unexpired jobPostings ordered by `coalesce(postedAt, _createdAt) desc` |
| contact-route lookups (2) | POST /api/contact | per-block `destinationEmailOverride` by `pageId`+`blockKey` (server-side, never client-trusted) and `schoolSettings.formEmail` fallback |

Field usage is per-field `consumedBy`/`unused` in the JSON. The live dataset uses 24 block types (121 block instances); 21 registered blocks are dead in live data. Draft mode: `frontend/app/api/draft-mode/enable/route.ts`, Presentation tool, `defineLive`, `DraftModeToast.tsx`, `VisualEditing`, `PageBuilder.tsx`/`BlockRenderer.tsx`. No `/api/revalidate` route.

## Phase 3 — Assets and images

Queries preserve image references/crop/hotspot; the only asset dereference is `favicon.asset->url` (unset live — `app/icon.svg` is the effective favicon). `SanityImage.tsx`, `image.ts`, `api.ts`, and `PortableText.tsx` are byte-identical to Wags: sanity-image srcset wrapper, OG 1200×627 crop, lightbox 1600 auto with dimensions parsed from the `_ref` regex. New: `urlForImage(logo).width(600)` for Organization JSON-LD (root layout) and `.width(360)` for the school header — **this fixes the Wags/Hound lineage bug** that emitted raw `_ref` strings as JSON-LD image URLs (both try/catch-guarded; currently moot for the resort since `settings.logo` is unset). Production holds 93 image assets, 0 file assets. Public assets include `/images/riverside-logo-2026.png` and `/static/page-builder-thumbnails/*`.

## Phase 4 — Structural and relational map

```text
settings --nav/footer/button links--> page | service
schoolSettings --nav/footer/CTA links--> schoolPage (via link objects)
page | service | schoolPage | locationPage --pageBuilder--> inline block objects
schoolPage.pageBuilder ONLY --> quickSchoolsEnquiry | jobListings
jobListings --GROQ subquery (not a reference)--> jobPosting
serviceTabs.tabs[] --ref--> service      testimonials.reviews[] --ref--> testimonial
blockContent/link/button --internal links--> page | service
```

Routing: `/` → page slug `homepage`; `/[slug]` → pages; `/services/[slug]` → services; `/locations/[suburb]` → locationPages (segment named `suburb`, mapped from slug); `/school` → schoolPage slug `home`; `/school/[slug]` → other schoolPages. All five dynamic routes implement `generateStaticParams` (school excludes `home`). Navigation is CMS-authored and the layout injects fetched service children when a nav label is exactly `Services` — live settings uses that label, so the lineage magic-label path is **live**. SEO: per-document `seo` objects, per-route canonical alternates (new vs Wags), FAQPage JSON-LD from live faqAccordion blocks on `/[slug]` pages (`buildFaqPageJsonLd`/`collectFaqs` in `sanity/lib/utils.ts`), root defaults from settings. Sitemap/robots are code-generated against `SITE_URL`, always emitting the canonical host. Redirects: `redirect-map.ts` defines legacy-host 301s (riogrooming.com, riogroomingschool.com; deliberately excluding multi-location barksnrec.co) — wired via `frontend/proxy.ts` (Next 16's rename of `middleware.ts`); `next.config.ts` has no redirects. (An earlier version of this audit wrongly reported the map as unwired.)

## Phase 5 — Live dataset snapshot

Read-only queries ran against project `7ze0boy4`, dataset `production`, on 2026-07-11 (Sanity MCP, authenticated). Counts: page 6 (homepage, about, careers, contact, faq, gallery), service 3 (boarding, daycare, grooming), schoolPage 10 (home, career-placement, enrollment-financing, gallery, job-listings, request-information, schedule-a-tour, scholarships, student-housing, why-become-a-groomer), locationPage 7 (afton-mn, cottage-grove-mn, ellsworth-wi, hastings-mn, lakeville-mn, prescott-wi, woodbury-mn), jobPosting 0, testimonial 0; 93 images, 0 files; 134 docs total. The single draft is a `sanity.previewUrlSecret` system record. No orphaned content types.

Live block counts (121 total): iconGrid 20; ctaStrip, splitContent, heroMinimal 12 each; heroSplit 10; faqAccordion 9; infoSection 7; processSteps 6; contactForm, whatsIncluded 4 each; spacer, serviceCards, contentColumns, pricingList 3 each; galleryPage, photoMarquee, heroMarquee 2 each; one each of galleryCarousel, featureGrid, featureList, jobListings, pricingMatrix, quickSchoolsEnquiry, valuePillars.

Notable live operational values: `posUrls` carries real Goose booking URLs (`booking.goose.pet/riverside-pet-resort/search/{boarding,daycare}/pets`); `gtmContainerId` and `ga4MeasurementId` are both **unset** (no analytics fire in production); `transitionBanner.enabled` is false; four live `destinationEmailOverride` values route forms to riversidepetmn.com and riogrooming.com addresses; `schoolSettings.formEmail` is `amy@riogrooming.com` (no `[EMAIL-TBD]` markers remain anywhere).

### Settings integrity

| Type | `_id` | created | updated |
|---|---|---|---|
| settings | `siteSettings` | 2026-06-15T15:24:13Z | 2026-06-26T14:35:26Z |
| schoolSettings | `schoolSettings` | 2026-06-17T22:37:04Z | 2026-07-01T19:26:00Z |

Exactly one document per singleton, both at their Studio-pinned ids — **clean**, the first repo in the lineage without the competing-documents defect (`scripts/migrate-settings-id.js` shows deliberate consolidation). Both frontend queries remain unordered `[0]` (latent). Diagnosis only; nothing was mutated.

## Phase 6 — Migration risk register

| Priority | Severity | Flag | Evidence |
|---:|---|---|---|
| 1 | ~~high~~ FALSE POSITIVE (corrected 7/26) | redirect-map.ts IS wired via `frontend/proxy.ts` (Next 16 renamed middleware.ts → proxy.ts); the audit grepped for the old filename | `frontend/proxy.ts` imports `isLegacyHost` + `resolveLegacyRedirect` |
| 2 | high | Canonical domain UNCONFIRMED (riversidepetmn.com vs "RiversidePet.com", pending Peter) | `frontend/app/site-config.ts`; `.env.example` |
| 3 | medium | 29 production write scripts w/ hardcoded project id — dataset is source of truth, not Studio history | `scripts/` (28 of 29 embed `7ze0boy4`) |
| 4 | medium | PT link marks unrendered at seven bare render sites (widened from five) | `PortableText.tsx` + seven bare `@portabletext/react` imports |
| 5 | medium | jobListings embeds a cross-document GROQ join inside pageBuilder — unique in the portfolio; the adapter must reproduce a runtime join | `queries.ts` pageBuilderExpansion |
| 6 | medium | Contact API reads CMS at request time (override + school formEmail, revalidate 300) | `app/api/contact/route.ts` |
| 7 | medium | Presentation/live-editing coupling to delete at migration | `live.ts`, `client.ts`, layout, draft-mode route |
| 8 | medium | Image transform surface must be reproduced exactly (srcset wrapper, OG 1200×627, lightbox 1600 + `_ref` dimension regex, logo 600/360) | `SanityImage.tsx`, `utils.ts`, `image.ts`, layouts |
| 9 | medium | Resort logo hardcoded; CMS logo field unset and unconsumed for display (Kingdom defect re-appeared) | `Header.tsx:219`, `Footer.tsx:58` |
| 10 | low | GTM/GA4 unset live; settings has NO ctmScriptUrl field (removed vs Wags) — no CMS home for CallTrackingMetrics | snapshot; `settings.tsx` diff |
| 11 | low | Unordered `[0]` singleton selection on both settings types (latent) | `queries.ts`; snapshot |
| 12 | low | Hardcoded calculator prices, header booking CTAs; 21 of 45 registered blocks dead in live data | `pricingData.ts`; `(site)/layout.tsx`; block counts |

### Cross-contamination sweep

The case-insensitive repo sweep over `frontend/app`, `frontend/sanity`, and `studio/src` found **zero** other-portfolio brand names, domains, phones, or addresses in runtime code — the cleanest result in the lineage. Embark references are intentional schema-description examples (`settings.tsx:267,273`, `splitContent.ts:34`). Both `HeroMarquee.tsx` fallback alt strings are `Riverside Pet Resort` (lines 121, 341) — the historic Boxers fallback did not propagate. Portfolio names in governance/context docs (`CLAUDE.md`, `AGENTS.md`, `context/`, `new-context/`) are internal authoring text, low severity.

### Previous-owner / legacy-infrastructure sweep (Riverside-specific)

Riverside consolidates two predecessor businesses: Rio Grooming (riogrooming.com / riogroomingschool.com) and Barks & Rec Hastings. Legacy references are concentrated where expected: `redirect-map.ts` (crawl-derived 301 pairs from riogrooming.com, 6/20) with an explicit note that `barksnrec.co` must NOT be host-redirected (it remains a live multi-location site; only `/hastings` becomes Riverside, redirected on Peter's side — confirmed 6/26); `new-context/` retains the content-extraction archive; live form destinations still use `@riogrooming.com` mailboxes; the QuickSchools tenant is `riogran`. The `transitionBanner` settings object exists precisely to announce this transition (currently disabled). No legacy runtime dependency was found; the school's public traffic depends on the unwired redirect map at cutover — see risk 1.

## Evidence boundary

Dataset claims are from read-only GROQ queries executed 2026-07-11 via the authenticated Sanity MCP connector. Drift claims are from mechanical `diff`/`md5` against `/Users/michaellaufersweiler/Desktop/lauf/dev/client-websites/wags-stay-n-play` and the reference JSON under `cms-audit/reference/`. No application, schema, dataset, DNS, or infrastructure changes were made.
